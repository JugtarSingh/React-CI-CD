name: "sample react ci pipepline"

on:
    push:
        branches:
            - master
            - main

jobs:
    built-and-test:
        runs-on: ubuntu-latest

    steps:
          name: "setup node JS"
          uses: actions/setup-node@v4
        


